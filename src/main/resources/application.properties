# === CONFIGURACIÓN GENERAL ===
spring.application.name=sistema-turnos
server.port=8080

# === DATOS DE CONEXIÓN A MYSQL ===
spring.datasource.url=jdbc:mysql://localhost:3306/sistema_turnos?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Alct1023*

# === JPA / HIBERNATE ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# === FORMATO BONITO DE LAS CONSULTAS SQL (opcional) ===
spring.jpa.properties.hibernate.format_sql=true

# === JWT CONFIG ===
# Clave secreta en Base64 (IMPORTANTE: no se pone una clave simple tipo "1234", debe ser al menos 256 bits en base64)
jwt.secret=N2hkTnN5d2VkaXJyU1h3Z2Z4Z0NvT0N3OGYxZmtRaGg=

# Tiempo de expiración del token en milisegundos (ejemplo: 1 hora)
jwt.expiration=3600000

